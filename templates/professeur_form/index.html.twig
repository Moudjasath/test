<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Document</title>
        <link rel="stylesheet" href="{{asset('css/bootstrap.css')}}">
        <link rel="stylesheet" href="{{asset('style.css') }}">
</head>
    <body>
            <nav class="navbar navbar-light bg-light">
            <div class="container-fluid">
            <a class="navbar-brand">Professeur</a>
            <form class="d-flex">
            <button class="btn btn-outline-success" type="submit">Connexion</button>
                <button class="btn btn-outline-success" type="submit">Inscription</button>
            </form>
            </div>
        </nav>
    
<div class="container mt-5">
   
    {{ form_start(professeur) }}

     <div class="row">
        <div class="col-6">
            {{form_row(professeur.nomProf)}}
        </div>
        <div class="col-6">
            {{form_row(professeur.prenomProf)}}
        </div>
    </div>
       <div class="row">
            <div class="col-6">
               {{form_row(professeur.age)}}
            </div>
            <div class="col-6">
              {{form_row(professeur.sexe)}}
            </div>
        
        </div>
        <div class="row">
            <div class="col-6">
               {{form_row(professeur.telephone )}}
            </div>
            <div class="col-6">
              {{form_row(professeur.filiere)}}
            </div>
        
        </div>
                <div class="row">
                    <div class="col-6">
                    {{form_row(professeur.matiere)}}
                    </div>
                    
                
                </div>

        
                <div class="col-12 mt-4 d-flex justify-content-center">
                    <button class="btn btn-outline-success" type="submit">Enregistrer</button>
                </div>
        
     {{ form_end(professeur) }}
   </div>

    
    </body>
</html>
